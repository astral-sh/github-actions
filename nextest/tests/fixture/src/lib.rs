#[cfg(test)]
mod tests {
    #[test]
    fn one() {
        assert_eq!(1 + 1, 2);
    }
    #[test]
    fn two() {
        assert_eq!(2 + 2, 4);
    }
    #[test]
    fn three() {
        assert_eq!(3 + 3, 6);
    }
    #[test]
    fn four() {
        assert_eq!(4 + 4, 8);
    }
}
